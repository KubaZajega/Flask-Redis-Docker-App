# Flask‑Redis‑Docker‑App

A simple Flask application using Redis, containerized with Docker and Docker Compose.  
This project was created **as part of university exercises**.

## 🚀 Features

- Flask web application  
- Integration with Redis for data storage  
- Dockerized with a **Dockerfile**  
- Multi-container setup using **docker-compose**  
